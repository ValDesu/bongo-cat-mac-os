# Bongo Cat macOS Configuration Example
# Copy this file to 'settings.env' and edit the values to customize the application

### Window settings
# Keep window always on top (true/false)
ALWAYS_ON_TOP=false
# Remove window decorations for transparency (true/false)
REMOVE_DECORATIONS=true

### Appearance settings
# Theme folder name (default, nyao, or custom folder in img/)
THEME=default
# Scale factor for the window size (1 = original size)
SCALE=1
